import React from 'react';

type P = {
  output: number;
};

class Calculator extends React.Component<P> {
  constructor(props: P) {
    super(props);
  }

  render() {
    return <div></div>;
  }
}

export default Calculator;
